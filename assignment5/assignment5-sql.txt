# Task 1: total price of first 5 orders, ordered by order_id
SELECT orders.order_id, SUM(line_items.quantity * products.price) AS total_price FROM (SELECT order_id FROM orders ORDER BY order_id ASC LIMIT 5) orders JOIN line_items ON orders.order_id = line_items.order_id JOIN products ON line_items.product_id = products.product_id GROUP BY orders.order_id ORDER BY orders.order_id ASC;

# Task 2: average order price per customer
SELECT customer_name, AVG(t.total_price) AS average_order_price FROM customers c JOIN (SELECT orders.order_id, orders.customer_id,  SUM(products.price * line_items.quantity) AS total_price FROM orders JOIN line_items ON orders.order_id = line_items.order_id JOIN products ON line_items.product_id = products.product_id GROUP BY orders.order_id, orders.customer_id) AS t ON c.customer_id = t.customer_id GROUP BY c.customer_id, customer_name ORDER BY customer_name;

# Task 3: create a new order for Perez and Sons
SELECT customer_id FROM customers WHERE customer_name = 'Perez and Sons';
SELECT employee_id FROM employees WHERE first_name = 'Miranda' AND last_name = 'Harris'; 
SELECT product_id FROM products ORDER BY price ASC LIMIT 5;
BEGIN; 
INSERT INTO orders (customer_id, employee_id, date) VALUES ((SELECT customer_id FROM customers WHERE customer_name = 'Perez and Sons'), (SELECT employee_id FROM employees WHERE first_name = 'Miranda' AND last_name = 'Harris'), '2025-03-11') RETURNING order_id;
INSERT INTO line_items (order_id, product_id, quantity) VALUES (250, 23, 10), (250, 18, 10), (250, 43, 10), (250, 9, 10), (250, 44, 10);
COMMIT;
SELECT * FROM line_items WHERE order_id = 250; 

# Task 4: employees with more than 5 orders
SELECT employees.first_name, employees.last_name, COUNT(orders.order_id) AS order_count FROM employees JOIN orders ON employees.employee_id = orders.employee_id GROUP BY employees.employee_id, first_name, last_name HAVING COUNT(orders.order_id) > 5 ORDER BY last_name; 

# customer_id(Perez and Sons)
# 16

# employee_id(Miranda Harris)
# 7

# product_id
# 23
# 18
# 43
# 9
# 44

# order_id
# 250

